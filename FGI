#!/data/data/com.termux/files/usr/bin/bash
# Wrapper pour FGI.so
cd /storage/emulated/0/Download/cythonize
echo "========================================="
echo "🚀 FGI - Module compilé"
echo "========================================="
echo "📊 1223 lignes de code"
echo ""

if [ "$1" = "list" ]; then
    echo "📋 Fonctions disponibles:"
    python -c "import FGI; print([f for f in dir(FGI) if not f.startswith('_')])"
elif [ -n "$1" ]; then
    echo "🚀 Appel de $1()"
    python -c "import FGI; getattr(FGI, '$1')()"
else
    echo "🚀 Exécution par défaut"
    python -c "import FGI"
fi

echo ""
echo "========================================="
